ДЕТСКАЯ ЛИГА API - OpenAPI 3.0.3 Documentation
================================================

Directory: /sessions/zealous-compassionate-curie/mnt/leum/docs/api/

Files Created (3,078 lines of YAML):
====================================

1. openapi.yaml (3,792 bytes)
   - Main entry point for the API specification
   - References all sub-files using JSON Pointer syntax
   - Defines API metadata, tags, paths, and security schemes
   - Base URL: /api/v1
   
2. auth.yaml (5,868 bytes)
   - POST /auth/register - User registration with validation
   - POST /auth/login - Authentication with email/password
   - POST /auth/logout - Session termination
   - Response: User object + Bearer token

3. users.yaml (12,104 bytes)
   - GET /me - Current authenticated user profile
   - GET /users - Paginated user list with search
   - GET /users/{id} - User details with profiles
   - PUT /users/{id} - Profile update
   - POST /users/{id}/player-profile - Create/update player attributes
   - POST /users/{id}/coach-profile - Create/update coach attributes
   - Includes schemas for PlayerProfile, CoachProfile, TeamMembership

4. clubs.yaml (8,175 bytes)
   - GET /clubs - List with filters (sport, country, city, search)
   - POST /clubs - Create with multipart logo upload
   - GET /clubs/{id} - Club details with relationships
   - PUT /clubs/{id} - Update (supports multipart)
   - DELETE /clubs/{id} - Remove club (204 No Content)

5. teams.yaml (9,327 bytes)
   - GET /clubs/{clubId}/teams - List teams by club
   - POST /clubs/{clubId}/teams - Create team
   - GET /teams/{id} - Team details
   - PUT /teams/{id} - Update team
   - DELETE /teams/{id} - Remove team
   - POST /teams/{teamId}/members - Add members with roles

6. trainings.yaml (11,572 bytes)
   - GET /trainings - List with date range and status filters
   - POST /trainings - Schedule training session
   - GET /trainings/{id} - Training details with attendance
   - PUT /trainings/{id} - Update training
   - POST /trainings/{id}/cancel - Cancel with reason
   - PATCH /trainings/{trainingId}/attendance/{playerUserId} - Mark attendance

7. matches.yaml (15,291 bytes)
   - GET /matches - List with team/tournament/date filters
   - POST /matches - Create match fixture
   - GET /matches/{id} - Match details with events and lineup
   - PUT /matches/{id} - Update match
   - POST /matches/{id}/start - Start match (sets actual_start_at)
   - POST /matches/{id}/end - End match with final score
   - POST /matches/{id}/events - Record events (goals, cards, etc.)
   - PUT /matches/{id}/lineup - Set team lineup and positions

8. tournaments.yaml (10,704 bytes)
   - GET /tournaments - List with sport type and year filters
   - POST /tournaments - Create tournament
   - GET /tournaments/{id} - Tournament details
   - PUT /tournaments/{id} - Update tournament
   - DELETE /tournaments/{id} - Remove tournament
   - POST /tournaments/{id}/teams - Register teams in tournament

9. references.yaml (7,948 bytes)
   - GET /refs/sport-types - List sports (no auth required)
   - GET /refs/club-types - List club types (no auth required)
   - GET /refs/user-roles - List roles (no auth required)
   - GET /refs/positions - List positions with sport filter (no auth)
   - GET /refs/dominant-feet - List foot dominance options (no auth)
   - GET /refs/kinship-types - List relationship types (no auth)
   - GET /refs/match-event-types - List event types (no auth)
   - GET /refs/countries - List countries with search (no auth)
   - GET /refs/cities - List cities with country filter (no auth)

Total Statistics:
=================
- 9 YAML specification files
- 3,078 lines of YAML code
- 82 KB total size
- 40+ endpoints documented
- 50+ reusable schemas defined
- 100% valid YAML syntax
- Full Russian language support

Key Features Implemented:
=========================
✓ RESTful API design (GET, POST, PUT, PATCH, DELETE)
✓ Pagination with meta information
✓ Bearer token authentication (Laravel Sanctum)
✓ File uploads (multipart/form-data)
✓ Request/response validation schemas
✓ HTTP status codes (200, 201, 204, 401, 404, 422)
✓ Inline request/response examples
✓ Relationship handling (nested objects)
✓ Filtering and search capabilities
✓ Date/time format support
✓ Nullable fields and optional parameters
✓ Enum constraints for specific fields

Authentication:
================
All endpoints except /refs/* require:
- Header: Authorization: Bearer <token>
- Token format: Laravel Sanctum compatible
- Generated on /auth/login or /auth/register

Usage Examples:
===============
1. Swagger UI: Import openapi.yaml into Swagger Editor
2. Postman: Import as OpenAPI 3.0 specification
3. Code Generation: Use with openapi-generator-cli
4. Documentation: Publish with Swagger UI or ReDoc
5. Testing: Use with Postman, Insomnia, or REST Client

Path Structure:
================
/api/v1/
├── /auth/*              (Registration, Login, Logout)
├── /users/*             (User management, profiles)
├── /clubs/*             (Club management)
├── /clubs/{id}/teams/*  (Teams under club)
├── /teams/*             (Team details, members)
├── /trainings/*         (Training management)
├── /matches/*           (Match management, events)
├── /tournaments/*       (Tournament management)
└── /refs/*              (Public reference data)

All files are UTF-8 encoded with proper YAML syntax validation.
